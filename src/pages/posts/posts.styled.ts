import { COLORS, SPACES } from "src/consts";
import styled from "styled-components";


export const Header = styled.div`
  color:${COLORS.PRIMARY};
  font-size: 30px;
  font-weight: 800;
  margin-bottom: ${SPACES.l};
`